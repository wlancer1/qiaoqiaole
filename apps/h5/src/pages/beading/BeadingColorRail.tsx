export function BeadingColorRail({ requirements, completed, current, onSelect, onCompleteColor }: { requirements: Array<{ colorCode: string; required: number }>; completed: string[]; current: string | null; onSelect: (colorCode: string) => void; onCompleteColor: () => void }) {
  return <section className="beading-color-section" aria-label="拼豆色号进度"><div className="beading-color-rail">{requirements.map((item) => { const done = completed.includes(item.colorCode); return <button type="button" key={item.colorCode} className={`beading-color-chip ${current === item.colorCode ? 'is-current' : ''} ${done ? 'is-complete' : ''}`} aria-label={`${item.colorCode}${done ? '已完成' : '未完成'}`} onClick={() => onSelect(item.colorCode)}><strong>{done ? '✓' : item.colorCode}</strong><span>{item.required}颗</span></button>; })}</div><button type="button" className="beading-complete-color" onClick={onCompleteColor}>完成当前色</button></section>;
}
