# qiaoqiaole 开发约定

## 弹窗与抽屉

所有弹窗、底部抽屉、确认框和操作面板都必须遵守以下规则：

* 跨页面共用的弹窗必须渲染在不会随当前页面分支卸载的应用级覆盖层中，并与页面内容保持同级；不能挂在上一个页面或某个条件页面分支内部。
* 弹窗打开时锁定背景滚动，并阻止背景页面点击、触摸和返回操作穿透。
* 遮罩层点击只负责关闭当前弹窗；内容区域必须阻止事件传播。
* 弹窗关闭后恢复页面滚动和交互状态。
* 确认、取消、关闭、遮罩点击和异步提交都要有明确行为；支持键盘操作的场景还要处理 Esc。提交期间禁止重复提交。
* 新增弹窗必须补充打开、取消、确认和背景点击不穿透的回归测试。
* 破坏性操作使用统一的 `ConfirmDialog`，不要调用浏览器原生 `alert` 或 `confirm`。

## 整体设计尺寸规范

### 适配范围与基础单位

* 根字号按 320–414 CSS px 的手机宽度标定；小于 320px 时保持 `24.32px`，大于 414px 时保持 `31.5px`。页面还包含 `600px` 以上的宽屏布局，不能把 414px 当成页面最大宽度。
* `html` 必须沿用现有流式字号 `clamp(24.32px, 7.6087vw, 31.5px)`，不要在局部页面重设根字号。
* 设计稿中的 `rem` 以项目当前 `html` 字号为基准；新增 CSS 优先复用已有变量和同一 rem 比例，不要随意引入另一套缩放体系。
* `body` 的代码最小宽度是 `10.1587rem`，按最小根字号约为 247px；它不是 320px 设计宽度的另一种写法。窄屏内容仍需避免横向溢出。
* 全局字体使用 `PingFangSC`，回退到系统中文字体；正文默认字号为 `16px`。

### 颜色与基础 Token

全局编辑器区域优先使用 `apps/h5/src/styles.css` 中已有变量：

* 主文字：`--ink: #171819`
* 次要文字：`--muted: #6f7478`
* 浅色背景：`--soft: #f7f7f3`
* 卡片背景：`--panel: #ffffff`
* 分割线：`--line: #deded9`
* 网格线：`--grid-line: #d9dcda`
* 主蓝色：`--blue: #208edc`
* 遮罩：`--sheet-backdrop: rgba(17, 24, 39, 0.38)`

主页、作品列表和分图流程使用其现有的 `--flow-*` Token（如 `--flow-brand: #146cff`、`--flow-bg: #f4f7fc`、`--flow-text: #101828`）。不要强行把 `--blue` 和 `--flow-brand` 合并，也不要在同一组件中无理由混用两套 Token。

### 间距、圆角和阴影

* 优先复用现有 `.127rem`、`.1905rem`、`.254rem`、`.3175rem`、`.4444rem`、`.5714rem` 等间距级别；同一组件内不要混入无依据的近似值。
* 紧凑控件常用 `.3175rem` 圆角，输入框和操作按钮常用 `.4444rem`，普通卡片常用 `.5714rem` 左右；具体尺寸必须先对照相邻同级组件。
* 保存、编辑资料、新建文件夹等居中表单弹窗沿用 `save-project-modal` / `save-project-panel`：面板宽度 `min(100%, 15.2381rem)`、圆角 `.9524rem`，并复用其内边距、阴影和动效。
* 统一确认提示框沿用 `ConfirmDialog`：宽度 `min(100%, 11rem)`、圆角 `.78rem`、双列按钮高度 `1.15rem`；不要套用保存弹窗的宽度和按钮尺寸。
* 底部 Sheet 使用 `--sheet-width: 12.127rem`、`--sheet-radius: .6984rem` 和 `--sheet-shadow`，典型实际宽度为 `min(calc(100% - .7619rem), var(--sheet-width))`；底部圆角保持为 0。

### 控件尺寸与可访问性

* 项目使用随根字号缩放的紧凑控件体系，不要把 `44px × 44px` 作为所有按钮的全局最小尺寸。
* 标签、作品文件夹筛选等紧凑胶囊按钮使用 `min-height: 1.016rem`、`font-size: .381rem`；同一横向列表里的新增按钮必须保持相同高度。
* 保存弹窗输入框使用 `1.65rem`，主提交按钮使用 `1.5873rem`；统一确认框按钮使用 `1.15rem`。这些是不同组件族，不能互相替换。
* 底部操作 Sheet 中的大操作卡片使用 `2.2857rem` 左右的最小高度；标题栏关闭按钮应与该 Sheet 的现有标题栏对齐。
* `44px` 是少数现有拼豆控件和操作面板关闭按钮的固定尺寸，不是全局 Token。新增控件先对齐所在页面的同级组件，不能仅因为是按钮就设为 `44px`。
* 所有弹窗使用 `role="dialog"` 或 `role="alertdialog"`、`aria-modal="true"`，并提供标题或 `aria-label`。
* 所有关闭、返回、确认和取消按钮必须有明确的中文可访问名称。
* 保留 `:focus-visible` 轮廓，不要通过 `outline: none` 隐藏键盘焦点。

### 画布与标尺

* 编辑器画布单格基础尺寸为 `14px`。
* 画布标尺预留槽宽为 `22px`。
* 图片或画布放大且超出工作区时，行列数字才贴着工作区边缘显示；未放大或完整可见时保持原有标尺。
* 标尺数字必须根据可见格子的实际屏幕中心计算，不能用近似平均位置，保证行列数字准确。
* 拼豆页面和编辑器页面的标尺都要遵守上述规则，但不能破坏各自的拖拽、缩放和标记交互。

### 安全区、层级和滚动

* 普通长页面使用 `min-height: 100svh`，需要锁定在视口内的编辑器、拼豆或分图页面使用 `height: 100dvh`；不要在同一场景中随意互换。
* 贴近设备边缘的顶部栏、底栏和横向工具区，只对实际贴边方向叠加对应的 `env(safe-area-inset-*)`，不要给不贴边的方向机械添加安全区。
* 弹窗打开时禁止背景滚动；弹窗内部内容超出时只允许弹窗内容滚动。
* 弹窗层级必须高于页面内容、工具栏和画布控件；新增层级先检查现有 z-index，避免无意义地继续增加。
* 背景页面不能因为弹窗按钮的冒泡而触发返回、卡片打开、提交或导航。

## 代码与验证

### Git 与工作区

* **开发过程中不要创建新的 Git 分支。**
* 默认直接在当前已检出的分支和当前工作区中完成开发；除非用户明确要求，否则不要执行 `git checkout -b`、`git switch -c`、`git branch <name>`、创建 worktree 或其他会新增开发分支的操作。
* 如果当前功能已经位于指定分支或 worktree 中，继续使用现有分支和工作区，不要为了任务拆分再额外创建分支。
* 保留工作区中已有的用户修改，不使用 `git reset --hard`、`git checkout -- .`、`git restore .`、强制切换分支等破坏性 Git 操作覆盖它们。
* 修改前先检查当前工作区状态，区分已有用户修改与本次任务修改；不得擅自删除、回滚或覆盖与本次任务无关的变更。

### 开发与验证

* 修改前先定位根因，优先补充最小回归测试，再修改实现。
* H5 相关修改至少运行对应 Vitest 测试和 `npm run build:h5`。
* 提交前运行 `git diff --check`。
* 项目当前没有 `lint` script，不要假设存在 `npm run lint`。
* 如果由于仓库当前已有错误导致完整测试或构建失败，需要明确区分：

  * 本次修改引入的失败；
  * 修改前已经存在的失败。
* 不要为了让测试通过而修改与当前任务无关的业务逻辑或测试。

### 页面提示与异步请求

* `H5App` 的全局提示（`status`）必须绑定当前 Router 作用域（由 `location.key + pathname + search` 组成）；路由变化时立即清理旧提示。
* 页面请求、异步操作和组件回调不能在页面离开后继续把错误或成功提示写入新页面；异步回调需要使用请求序号、取消标记或页面作用域校验丢弃过期结果。
* 页面级提示必须绑定当前 Router scope；优先使用页面自己的 `error` / `status` 状态。真正跨路由的全局通知可以使用独立的全局通知状态，但必须提供独立清除行为，不得与页面提示混淆；如果使用应用级提示，必须补充跨页面切换和旧请求晚到的回归测试。
* 不要把上一页面的提示作为下一页面的初始状态；异步回调派发提示时必须使用创建时捕获的 Router 作用域；导航相关测试应明确断言新页面不会显示旧页面文案。

## 页面导航与路由

* 页面级导航必须使用 React Router，不要新增依赖 `screen`、`activeTab` 等 React state 来决定当前页面。
* `BrowserRouter` 是 H5 应用的统一路由入口；页面跳转使用 `navigate`、`Link` 或 `NavLink`，浏览器前进、后退和刷新必须保持可用。
* 路径、资源 ID、Tab、筛选条件、分页等可分享导航状态由 React Router 管理；服务端数据和缓存由 RTK Query 管理；草稿、弹窗、拖拽、撤销重做和异步加载等短生命周期状态继续使用组件 state 或合适的 Context。
* 新增页面必须先在 H5 路由映射中注册明确路径，再实现页面组件；禁止继续向 `H5App.tsx` 添加基于 `if (screen === ...)` 的页面分支作为新的导航方式。
* 路由参数必须经过编码和校验；需要资源 ID 的页面不能依赖上一个页面残留的内存对象才能渲染，刷新或直接打开深链接时应按 ID 重新加载数据。
* 需要登录的路由在页面级处理认证和返回目标；未登录时使用现有登录弹窗或登录入口，不要让页面组件自行修改全局导航变量。
* 生产服务器必须将未知 H5 路径回退到 `index.html`，否则 BrowserRouter 的深链接刷新会返回 404；相关部署配置变更需要补充验证。

## 状态管理

* H5 全局状态只使用 Redux Toolkit 和 React Redux；新增全局状态不得引入其他状态管理方案。
* 服务端数据默认统一使用一个 RTK Query `apiSlice`，业务模块通过 injected endpoints 扩展，禁止为同一 API 创建平行的查询缓存。
* 页面和组件状态按职责分层：可分享导航状态由 React Router 管理，服务端数据和缓存由 RTK Query 管理，短生命周期状态由组件 state 或合适的 Context 管理。
* Redux state 和 Redux action payload 必须可序列化。
* DOM、ref、函数、事件对象、`File`、`ImageData`、`PointerEvent`、定时器句柄、动画帧句柄等禁止放入 Redux。
* 不要创建巨大的 `appSlice` 或巨大的全局 Context；状态应按领域拆分，并保持清晰的所有权边界。
* Redux 中跨组件或跨领域使用的派生数据必须放在 `selectXxx` selector 中；组件内部简单的展示计算可以保留在组件内。
* Action 默认描述领域事件或业务结果，不要把机械性的 `setXxx` 作为跨组件通信的默认方式。
* 画布 Pointer movement 等高频交互状态保持在页面或组件本地，只在稳定边界（如提交、结束拖拽、完成绘制）提交到 Redux 或服务端。
* 每次 Slice 或 Endpoint 迁移都必须按适用范围补充行为相关测试：Slice 至少覆盖 reducer 和 selector；涉及 RTK Query 缓存时覆盖缓存失效（invalidation）；异步请求覆盖成功、失败、取消等适用路径。

## API 列表与详情接口规范

所有会返回集合数据的接口都必须明确区分“列表摘要”和“单条详情”，禁止为了复用查询或格式化函数，把详情数据直接塞进列表响应。

### 列表接口

* 列表接口只查询和返回当前列表页面实际展示的字段：`id`、名称、尺寸、状态、时间、计数、作者摘要和缩略图等。
* 列表接口禁止读取或返回大字段：`canvasData`、`beadList`、原图 `sourceImage`、Base64 图片、完整富文本、完整评论内容和其他可延迟加载的数据。
* 查询 SQL 必须显式列出字段，禁止在列表接口使用 `SELECT *`。格式化函数也不能依赖列表中不存在的详情字段。
* 列表接口必须支持分页，默认只返回当前页面；前端禁止为了初始化页面循环请求所有分页。需要继续浏览时使用“加载更多”、触底加载或明确分页控件。
* 分页参数必须限制最大 `pageSize`，并返回当前页信息和是否还有更多数据；不能让客户端自行猜测或无限循环。
* 列表中的图片必须使用缩略图或稳定的资源地址，不能用原图作为默认兜底。Base64 图片不得作为常规列表数据存储或批量返回。

### 详情接口

* 用户点击编辑、查看详情、开始拼豆等明确操作时，才请求 `GET /resource/:id` 之类的详情接口。
* 详情接口可以返回 `canvasData`、`beadList`、原图地址和完整内容，但必须按资源 ID 查询并校验访问权限或资源归属。
* 列表和详情必须使用不同的查询函数或明确的字段投影，不能通过给列表查询追加大字段来满足详情场景。
* 详情接口返回的字段也应按页面用途拆分；评论、回复、豆子清单等可独立分页或延迟加载时，不要强行嵌入主详情响应。

### 统计、评论和性能检查

* 计数优先使用基于关联字段的子查询或预聚合字段，并确保关联字段有索引；不要为了 `COUNT` 把大表 `JOIN` 后再 `GROUP BY` 整个结果集。
* 评论接口校验资源是否存在或已发布时，只查询必要的 `id`、归属和状态字段，不得调用包含原图、画布或豆子清单的完整详情查询。
* 评论回复必须限制在当前页顶层评论的后代范围内；不能先读取整个资源的全部回复，再在应用层筛选。
* 新增或修改列表接口时，必须检查响应 JSON 大小、SQL 是否读取大字段、分页是否真正生效，以及前端是否只请求当前需要的页。
* 列表接口需要补充测试，明确断言大字段不出现在列表响应；详情接口需要补充测试，明确断言编辑所需数据仍可按 ID 获取。
* API 生产数据库使用原生磁盘 SQLite（WAL 模式）；禁止在服务端重新引入 `sql.js`、`db.export()` 或每次写操作覆盖整个数据库文件。数据库路径必须挂载持久化卷，并在迁移或部署前保留可恢复备份。
